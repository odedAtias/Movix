// RN core components & API imports
import { StyleSheet, Text, View } from 'react-native';

// ExploreStack component
const ExploreStack = () => {
	return (
		<View>
			<Text>ExploreStack</Text>
		</View>
	);
};

// ExploreStack StyleSheet
const styles = StyleSheet.create({});

export default ExploreStack;
